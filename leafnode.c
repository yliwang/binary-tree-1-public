#include <stdio.h>
#include <stdbool.h>
#include <assert.h>

#include "tree.h"

int get_num_leaf(struct binary_tree *tree)
{
	int num_leaf = 0;

	/* code for counting leaf nodes */

	return num_leaf;
}
